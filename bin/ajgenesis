#!/usr/bin/env node

"use strict";

var ajgenesis = require('ajgenesis'),
    path = require('path');

var model = { };

ajgenesis.process(model, process.argv.slice(2), function (err, result) {
    if (err)
        console.log(err);
});